import React from 'react';

const Projects = () => {
    const st = {
        marginTop: "100px",
        padding: "100px",
        textAlign: "center",
        color: "white",
    }
    return (
        <div className="" style={st}>
            <h1>This is Project Page</h1>
        </div>
    );
};

export default Projects;