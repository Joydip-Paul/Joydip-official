import React from 'react';
import './Contact.css';

const Contact = () => {
    const st = {
        marginTop: "100px",
        padding: "100px",
        textAlign: "center",
        color: "white",
    }
    return (
        <div className="" style = {st}>
            <h1>This is Contact Page</h1>
        </div>
    );
};

export default Contact;